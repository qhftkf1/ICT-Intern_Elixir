# It is for my Elixir study


# 7 Pipe Operator
# We can avoid getting messy in functions

  # foo(bar(baz(new_function(other_function()))))
  # other_function() |> new_function() |> baz() |> bar() |> foo()

  #"Elixir rocks" |> String.split()

dddd
